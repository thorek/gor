entity:
  AlphaBeta:
    belongsTo:
      - Alpha
      - Beta
    same:
      Delta:  # weil groß - foreignKey of Type, same as deltaId except check Delta exists
        - Alpha.Beta
        - Beta

