enum:
  ProcessingActivityStatus:
    DRAFT: draft
    ACTIVE: active
    INACTIVE: inactive

entity:
  ProcessingActivity:
    attributes:
      name: string
      description: string
      status: ProcessingActivityStatus
    assocFrom:
      - OrganisationalUnitProcessingActivity
    assocTo:
      - Organisation
      - AutomatedDescisionMaking
    seeds:
      madb:
        name: "Mitarbeiterdatenbank"
        Organisation: disphere
      kdb:
        name: "Kundendatenbank"
        Organisation: boring
